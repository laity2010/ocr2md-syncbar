# ocr2md-syncbar

macOS menu bar status component for the ocr2md iCloud ↔ Google Drive rclone bridge.

First implementation: SwiftBar reads the existing bridge log and reports sync status without changing the sync process.
